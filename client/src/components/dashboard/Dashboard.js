// fetch all of our data using an action and bring it in from the redux state and pass it down to other components ex, experience and education
import React from 'react';
import PropTypes from 'prop-types';

const Dashboard = (props) => {
  return <div>Dashboard</div>;
};

Dashboard.propTypes = {};

export default Dashboard;
